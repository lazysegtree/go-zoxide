package zoxide

import "time"

const defaultExecTimeout = 3 * time.Second
const commandName = "zoxide"
const noMatchError = "zoxide: no match found"
const scoreSeperator = " "
const defaultScore = 0.0
